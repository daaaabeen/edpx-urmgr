/**
 * @file ${modName} ${pageName} reducer
 * @author: dabeen(lidianbin@baidu.com
 */

import {actions, composeReducer} from 'ei';
import * as types from './actionTypes';

export default composeReducer({

    /**
     * demo
     */
    // xxx(state, action) {
    //     switch (action.type) {
    //         case actions.INIT:
    //             return action.payload.xxx;
    //         default:
    //             return state;
    //     }
    // }

});
